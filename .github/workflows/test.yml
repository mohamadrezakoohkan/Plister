name: Test

on:
   push:
      branches:
        - master

jobs:
  test:
    name: Test
    runs-on: macOS-latest
    strategy:
        matrix:
          destination: ['platform=iOS Simulator,OS=13.3,name=iPhone 11']

    steps:
      - name: Checkout
        uses: actions/checkout@master
      - name: Clean
        run: |
          xcodebuild clean
      - name: Build generic iOS devices
        run: |
          xcodebuild build CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ONLY_ACTIVE_ARCH=NO
      - name: Test on iOS 13.3
        run: |
          xcodebuild test -project Plister.xcodeproj -scheme Plister -destination "${destination}" CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ONLY_ACTIVE_ARCH=NO

        env:
         destination: ${{ matrix.destination }}
      - name: Update code coverage
        run: |
          bash <(curl -s https://codecov.io/bash) -J 'Plister'


